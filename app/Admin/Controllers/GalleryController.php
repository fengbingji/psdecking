<?php

namespace App\Admin\Controllers;

use App\Admin\Repositories\Gallery;
use Dcat\Admin\Form;
use Dcat\Admin\Grid;
use Dcat\Admin\Http\Controllers\AdminController;
use Dcat\Admin\Layout\Content;

class GalleryController extends AdminController
{
    protected $title = '相册';

    /**
     * Make a grid builder.
     *
     * @return Grid
     */
    protected function grid()
    {
        return Grid::make(new Gallery(), function (Grid $grid) {
            $grid->column('id')->sortable();
            $grid->column('title')->editable();
            $grid->column('tags')->label();
            $grid->column('images')->limit(2, '')->image(null, height: 150);
            $grid->column('locale');
            $grid->column('created_at');

            $grid->filter(function (Grid\Filter $filter) {
                $filter->equal('id');
            });

            $grid->model()
                ->whereJsonContains('tags', '相册')
                ->orderBy('id', 'desc');
        });
    }

    public function show($id, Content $content)
    {
        return parent::show($id, $content); // TODO: Change the autogenerated stub
    }

    /**
     * Make a form builder.
     *
     * @return Form
     */
    protected function form()
    {
        return Form::make(new Gallery(), function (Form $form) {
            $form->display('id');
            $form->text('title');
            $form->tags('tags')->value('相册');
            $form->multipleImage('images')
                ->sortable()
                ->maxSize(1024 * 3)
                ->uniqueName()
                ->autoUpload()
                ->scaleDown(1024, 960)
                ->thumbnail('s', 400, 400)
                ->toWebp(90);
            $form->editor('description');
            $form->radio('lang')->options(['zh' => '中文', 'en' => 'English'])->default('zh');

            $form->display('created_at');
            $form->display('updated_at');
        });
    }
}
